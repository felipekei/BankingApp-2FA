package com.example.BankingAppFB.model;

/**
 * TransactionType Enum
 * Defines a set of name constants, in this case transasction types
 * Represents the types of transaction that an user has performed on their account
 */

 public enum TransactionType {
    DEPOSIT,
    WITHDRAWAL,
    TRANSFER,
    BILL_PAYMENT
}